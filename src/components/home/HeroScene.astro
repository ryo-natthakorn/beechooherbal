---
// src/components/home/HeroScene.astro
// Hero — drenched in Harmony Green (brand-committed colour strategy: the primary colour
// carries the surface), with the guide's hand-drawn stroke line work behind and a layered
// brand-white/brand-yellow glow for depth (no off-brand hues). The two-tier H1 uses nested
// spans so the rendered hierarchy changes while the DOM text stays identical to the live
// site (SEO). TH spans force leading-normal (text-balance is unreliable with space-free
// Thai script) — see the class ternaries below.
import YouTubeEmbed from "../YouTubeEmbed.astro";
import HerbalStrokes from "../HerbalStrokes.astro";
import { HOME, type Lang } from "../../data/home";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = HOME.hero;
const serif = lang === "th" ? "font-serif-thai" : "font-serif";
---

<section class="relative flex min-h-[100svh] items-center overflow-hidden bg-brand-green pt-24 pb-14 text-center md:py-28">
  <div class="hero-aura pointer-events-none absolute inset-0" aria-hidden="true"></div>
  <!-- WebGL living-gradient layer. Transparent (opacity-0) until the shader renders its
       first frame, so mobile / reduced-motion / no-JS / no-WebGL visitors only ever see
       the .hero-aura beneath. Lazy-initialised by home-motion.ts (desktop-only gates). -->
  <canvas data-hero-canvas class="pointer-events-none absolute inset-0 h-full w-full opacity-0 transition-opacity duration-1000" aria-hidden="true"></canvas>
  <HerbalStrokes draw class="pointer-events-none absolute inset-0 h-full w-full text-white opacity-10" />
  <div class="hero-enter relative mx-auto w-full max-w-6xl px-4 md:px-8">
    <h1 class={`mx-auto max-w-4xl ${serif} font-bold text-white`}>
      <span class={lang === "en" ? "block text-2xl leading-snug sm:text-3xl md:text-balance md:text-5xl md:leading-tight" : "block text-2xl leading-normal md:text-4xl md:leading-normal"}>{t.h1Line1[lang]}</span>
      <span class={lang === "en" ? "mt-3 block text-base font-normal text-white/90 sm:text-lg md:text-2xl" : "mt-3 block text-lg font-normal leading-normal text-white/90 md:text-2xl md:leading-normal"}>{t.h1Line2[lang]}</span>
    </h1>
    <p class="mt-4 text-balance text-lg font-bold text-brand-yellow sm:text-xl md:mt-5 md:text-2xl">
      {t.tagline[lang]}
    </p>
    {/* "Find a Branch" (previously the first, white button here) dropped: it's always on
        screen in the header now (a yellow pill, every page, every scroll position), so
        repeating it in the hero was the same action twice before the visitor had
        scrolled at all. Facebook (already yellow) and LINE (already outlined) are
        unchanged. */}
    <div class="mt-6 flex flex-wrap justify-center gap-3 md:mt-8">
      <a href="https://www.facebook.com/beechooherbal/" target="_blank" rel="noopener" class="btn-tactile flex min-h-11 items-center gap-2 rounded-full bg-brand-yellow px-7 font-bold text-brand-ink shadow-soft-lg hover:bg-brand-yellow/90 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        <svg aria-hidden="true" viewBox="0 0 320 512" fill="currentColor" class="h-4 w-4 shrink-0">
          <path d="M279.14 288l14.22-92.66h-88.91v-60.13c0-25.35 12.42-50.06 52.24-50.06h40.42V6.26S260.43 0 225.36 0c-73.22 0-121.08 44.38-121.08 124.72v70.62H22.89V288h81.39v224h100.17V288z" />
        </svg>
        {t.ctaFacebook[lang]}
      </a>
      <a href="https://lin.ee/ll3injb" target="_blank" rel="noopener" class="btn-tactile flex min-h-11 items-center gap-2 rounded-full border border-white/60 px-7 font-bold text-white hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        <svg aria-hidden="true" viewBox="0 0 24 24" fill="currentColor" class="h-4 w-4 shrink-0">
          <path d="M12 3C6.48 3 2 6.58 2 11c0 2.61 1.61 4.93 4.11 6.41-.18.94-.66 2.49-1.51 3.99a.4.4 0 0 0 .5.55c2.11-.76 3.66-1.7 4.51-2.29.76.14 1.55.21 2.39.21 5.52 0 10-3.58 10-8s-4.48-8-10-8z" />
        </svg>
        {t.ctaLine[lang]}
      </a>
    </div>
    <div class="mx-auto mt-7 max-w-2xl overflow-hidden rounded-2xl shadow-soft-lg md:mt-10">
      <YouTubeEmbed id={t.videoId} title={t.videoTitle[lang]} />
    </div>
  </div>
  <!-- Decorative scroll cue (gold pulse falls along a hairline; static under reduced motion). -->
  <div class="pointer-events-none absolute inset-x-0 bottom-5 hidden justify-center md:flex" aria-hidden="true">
    <span class="scroll-cue-line"></span>
  </div>
</section>
