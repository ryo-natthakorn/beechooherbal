---
// src/pages/th/รีวิวทรีทเม้นท์ที่ดี.astro
// Thai Reviews page — the twin of
// src/pages/reviews-and-testimonials-of-bee-choo-origin-treatment.astro. Thin route
// shell — see the EN twin's header for the full rationale.
// Content is src/data/reviews.ts, transcribed verbatim from the live site
// (wp-json/wp/v2/pages/580, fetched live — this page was edited THE SAME DAY this
// batch was built, so even the Phase-1 snapshot would have been stale here).
// hreflang is automatic — this path already exists in src/i18n/pairs.ts.
import BaseLayout from "../../layouts/BaseLayout.astro";
import ReviewsPage from "../../components/reviews/ReviewsPage.astro";
import { REVIEWS_SEO } from "../../data/reviews";

const lang = "th" as const;
const { title, description } = REVIEWS_SEO;
---

<BaseLayout title={title[lang]} description={description[lang]} {lang}>
  <ReviewsPage {lang} />
</BaseLayout>
